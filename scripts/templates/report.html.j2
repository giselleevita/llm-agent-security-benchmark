<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title }}</title>
  <style>
    :root { --bg:#f5f7fa; --card:#ffffff; --ink:#17202a; --muted:#5d6d7e; --accent:#0b6e4f; --border:#d6dbdf; }
    body { margin:0; font-family: "IBM Plex Sans", "Segoe UI", sans-serif; background:var(--bg); color:var(--ink); }
    .wrap { max-width: 1100px; margin: 0 auto; padding: 24px; }
    .card { background:var(--card); border:1px solid var(--border); border-radius:12px; padding:18px; margin-bottom:16px; }
    h1, h2 { margin: 0 0 10px 0; }
    h1 { color: var(--accent); }
    table { width:100%; border-collapse: collapse; font-size: 14px; }
    th, td { border:1px solid var(--border); padding:8px 10px; text-align:left; }
    th { background:#eef2f7; }
    .grid { display:grid; grid-template-columns: 1fr 1fr; gap:12px; }
    .chart img { width:100%; border:1px solid var(--border); border-radius:8px; background:#fff; }
    .meta { display:grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap:8px; font-size:13px; color:var(--muted); }
    @media (max-width: 860px){ .grid, .meta{ grid-template-columns:1fr; } }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1>{{ title }}</h1>
      <p>Enterprise benchmark output for policy-enforced LLM agent tool runtime.</p>
      <div class="meta">
        <div><strong>Commit:</strong> {{ meta.get('git_commit', 'unknown') }}</div>
        <div><strong>Timestamp:</strong> {{ meta.get('timestamp_utc', 'unknown') }}</div>
        <div><strong>Platform:</strong> {{ meta.get('platform', 'unknown') }}</div>
        <div><strong>Python:</strong> {{ meta.get('python_version', 'unknown') }}</div>
        <div><strong>Seed:</strong> {{ meta.get('seed', 'unknown') }}</div>
        <div><strong>Policy hash:</strong> {{ meta.get('policy_hash', 'unknown') }}</div>
      </div>
    </div>

    <div class="card">
      <h2>Baseline/Experiment Summary</h2>
      <table>
        <thead><tr>{% for h in headers %}<th>{{ h }}</th>{% endfor %}</tr></thead>
        <tbody>
        {% for row in rows %}
          <tr>{% for col in row %}<td>{{ col }}</td>{% endfor %}</tr>
        {% endfor %}
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2>Threat Breakdown</h2>
      <table>
        <thead><tr>{% for h in threat_headers %}<th>{{ h }}</th>{% endfor %}</tr></thead>
        <tbody>
        {% for row in threat_rows %}
          <tr>{% for col in row %}<td>{{ col }}</td>{% endfor %}</tr>
        {% endfor %}
        </tbody>
      </table>
    </div>

    <div class="grid">
      {% for chart in charts %}
      <div class="card chart">
        <h2>{{ chart }}</h2>
        <img src="{{ chart }}" alt="{{ chart }}" />
      </div>
      {% endfor %}
    </div>
  </div>
</body>
</html>
