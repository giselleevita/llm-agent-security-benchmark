services:
  opa:
    image: ${OPA_IMAGE:-openpolicyagent/opa:1.6.0}
    command: ["run", "--server", "--addr=0.0.0.0:8181", "--log-format=json", "/policies"]
    ports:
      - "8181:8181"
    volumes:
      - ./policies:/policies:ro
